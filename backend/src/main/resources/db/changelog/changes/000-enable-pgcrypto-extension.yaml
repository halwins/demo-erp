databaseChangeLog:
  - changeSet:
      id: 000-enable-pgcrypto-extension
      author: halwins
      changes:
        - sql:
            sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
      runOnChange: false
